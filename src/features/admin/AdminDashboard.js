import React from 'react';

const AdminDashboard = () => (
  <div>
    <h2>Admin Dashboard</h2>
    <p>Manage users, points, rewards, and analytics here.</p>
  </div>
);

export default AdminDashboard;